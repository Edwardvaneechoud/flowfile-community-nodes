## What it does

This node compares different ml models to enable automatic selection and analysis which model to use. Supports both classification and regression problems. Includes automatic generated graphs.

## Inputs

Training data

## Settings

Define which columns should be used for prediction, which column contains the output and define the models and comparisons that should be done.
